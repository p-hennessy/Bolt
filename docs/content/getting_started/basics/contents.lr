_model: page
---
title: Basics
---
order: 2
---
article:

Bolt derives all functionality via plugins. A very basic plugin looks something like this:

```python
from bolt import Plugin
from bolt import command

class Example(Plugin):

    @command("hello")
    def ping(self, message):
        self.say(message.channel_id, "world!")
```
